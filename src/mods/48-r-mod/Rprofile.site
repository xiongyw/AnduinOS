# Set default CRAN mirror for all users
#options(repos = c(CRAN = "https://cran.rstudio.com/"))
options(repos = c(CRAN="https://mirrors.tuna.tsinghua.edu.cn/CRAN/"))

# Set default editor
options(editor = "vim")

# Custom welcome message
cat("Welcome to R!\n")

